import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SummaryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the second page!',
  },
});
